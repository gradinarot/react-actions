import React from "react";

const Button = () => {
  return (
    <button
      style={{ padding: "10px", backgroundColor: "brown", border: "none" }}
    >
      Click Me Now
    </button>
  );
};

export default Button;
